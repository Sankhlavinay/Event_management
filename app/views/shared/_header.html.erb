<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to 'Event-Management', root_path, class: 'navbar-brand text-decoration-none text-white' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure to sign out?" }, class: "nav-link active" %>
          </li>
          <li class="nav-item">
            <% if current_user.organizer? %>
              <%= link_to 'Events', booking_events_path, class: "nav-link active" %>
            <% else %>
              <%= link_to 'Events', events_path, class: "nav-link active" %>
            <% end %>  
          </li>
          <% if current_user.organizer? %>
            <li class="nav-item">
              <%= link_to 'Create Event', new_booking_event_path, class: "nav-link active" %>
            </li>
          <% end %>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign In', new_user_session_path, class: "nav-link active text-decoration-none" %>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "#{current_user.email}", edit_user_registration_path, class: "nav-link active" %>
          </li>
          <li class="nav-item">
            <small class="nav-link active"><%= "#{current_user.role}" %></small>
          </li>
        <% end %>
      </ul>
      </div>
    </div>
  </nav>